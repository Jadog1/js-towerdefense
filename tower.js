// TODO: Put everything related to towers here.
var Tower = (function() {

    /**
     * Constructs a new Tower object.
     */
    var init = function() {
        var hitpoints; // TODO: Initialization (config file, constant)

        var get_hitpoints = function() {
            return hitpoints;
        };
    };

    return init;
}());
